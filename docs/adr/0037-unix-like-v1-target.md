# Target Unix-like local development environments for v1

Tasker v1 will target Linux and macOS-style local development environments with POSIX shell and Git assumptions. Windows support is future work because validating the local worktree, pi launcher, and agent workflow loop matters more than absorbing cross-platform filesystem and shell complexity up front.
