# Make the Workpad Note a first-class singleton

Tasker will model each Task's Workpad Note as a first-class singleton with revision history rather than as an ordinary comment discovered by marker text. This matches Symphony's single-source-of-truth workflow for agent plans, acceptance criteria, validation, and handoff notes while avoiding noisy append-only progress comment streams.
